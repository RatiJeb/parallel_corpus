<p><%= t('invitation_email.greeting', email: @resource.email ) %></p>

<p><%= t('invitation_email.p1', root_url: root_url) %></p>

<p><%= link_to t('invitation_email.link'), accept_invitation_url(@resource, invitation_token: @token) %></p>

<% if @resource.invitation_due_at %>
  <p><%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %></p>
<% end %>

<p><%= t('invitation_email.p2') %></p>
