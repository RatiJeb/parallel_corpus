<%= render 'admin/shared/heading', name: t('admin.add_new_publishing') %>

<main>
  <div class="mx-auto max-w-screen-2xl sm:px-6 lg:px-8">
    <%= render('admin/shared/form',
               url: admin_publishing_index_path(locale: I18n.locale),
               method: :post,
               record: @publishing,
               back_url: admin_publishing_index_path
              ) %>
  </div>
</main>
