<%= render partial: "admin/shared/header" %>

<h1>
  <%= link_to 'ქვეკორპუსები', admin_supergroups_path %>
</h1>
<p>
  <%= link_to 'ახალი ქვეკორპუსის დამატება', new_admin_supergroup_path %>
</p>
<p>
  მონაცემთა ბაზაში <%= @supergroups.count %> ჩანაწერია
</p>

<table>
  <thead>
    <th>
    </th>
  </thead>
  <tbody>
    <% @supergroups.each do |supergroup| %>
      <tr>
        <td> <%= link_to supergroup.name_ka, admin_groups_path(supergroup_id: supergroup.id) %> </td>
        <td> <%= link_to supergroup.name_en, admin_groups_path(supergroup_id: supergroup.id) %> </td>
        <td> <%= supergroup.status %> </td>
        <td> <%= link_to 'edit', edit_admin_supergroup_path(id: supergroup.id) %> </td>
      </tr>
    <% end %>
  </tbody>
</table>
