<%= render 'admin/shared/heading', name: 'ჟანრის რედაქტირება' %>

<main>
  <div class="mx-auto max-w-screen-2xl sm:px-6 lg:px-8">
    <%= render('admin/shared/form',
               url: admin_genre_path,
               method: :put,
               record: @genre,
               back_url: admin_genres_path,
               delete_url: ->(record){ admin_genre_path(record) }
              ) %>
  </div>
<main>
