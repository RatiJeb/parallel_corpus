<%= render 'admin/shared/heading', name: 'ჯგუფის რედაქტირება' %>

<main>
  <div class="mx-auto max-w-screen-2xl sm:px-6 lg:px-8">

    <% crumbs = [] %>
    <% crumbs << { name: @group.supergroup.name_ka, url: admin_groups_path(supergroup_id: @group.supergroup.id) } %>
    <% crumbs << { name: @group.name_ka, url: admin_collections_path(group_id: @group.id) } %>
    <% crumbs << { name: 'რედაქტირება', url: edit_admin_group_path(group_id: @group.id) } %>

    <%= render 'admin/shared/breadcrumbs', crumbs: crumbs %>

    <%= render 'admin/groups/shared/form', url: admin_group_path, method: :put %>

    <%= render 'admin/shared/confirm_delete_modal', modal_title: 'გსურთ ჯგუფის წაშლა?', modal_text: 'ჯგუფი წაიშლება სრულად' %>
  </div>
<main>
