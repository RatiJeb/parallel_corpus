<%= render partial: "admin/shared/header" %>

<h1>
  <% if @supergroup %>
    <%= link_to @supergroup.name_ka, admin_groups_path(supergroup_id: @supergroup.id) %> >
  <% end %>
  <%= link_to 'ჯგუფები', admin_groups_path(supergroup_id: @supergroup.id) %>
</h1>
<p>
  <%= link_to 'ახალი ჯგუფის დამატება', new_admin_group_path(supergroup_id: @supergroup.id) %>
</p>
<p>
  მონაცემთა ბაზაში <%= @groups.count %> ჩანაწერია
</p>

<table>
  <thead>
    <th>
    </th>
  </thead>
  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td> <%= link_to group.name_ka, admin_collections_path(group_id: group.id) %> </td>
        <td> <%= link_to group.name_en, admin_collections_path(group_id: group.id) %> </td>
        <td> <%= group.status %> </td>
        <td> <%= link_to 'edit', edit_admin_group_path(id: group.id) %> </td>
      </tr>
    <% end %>
  </tbody>
</table>
