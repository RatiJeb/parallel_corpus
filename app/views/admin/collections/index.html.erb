<% if @group %>
  <h1>
    <%= @group.name_ka %>
  </h1>
<% end %>

<table>
  <thead>
    <th>
    </th>
  </thead>
  <tbody>
    <% @collections.each do |collection| %>
      <tr>
        <td> <%= link_to collection.name_ka, admin_text_blocks_path(:collection_id => collection.id) %> </td>
        <td> <%= link_to collection.name_en, admin_text_blocks_path(:collection_id => collection.id) %> </td>
        <td> <%= collection.status %> </td>
      </tr>
    <% end %>
  </tbody>
</table>
