<%= render 'admin/shared/heading', name: 'ტიპის დამატება' %>

<main>
  <div class="mx-auto max-w-screen-2xl sm:px-6 lg:px-8">
    <%= render('admin/shared/form',
               url: admin_types_path,
               method: :post,
               record: @type,
               back_url: admin_types_path
              ) %>
  </div>
<main>
