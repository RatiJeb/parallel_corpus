<%= render 'admin/shared/heading', name: 'ტიპის რედაქტირება' %>

<main>
  <div class="mx-auto max-w-screen-2xl sm:px-6 lg:px-8">
    <%= render('admin/shared/form',
               url: admin_type_path,
               method: :put,
               record: @type,
               back_url: admin_types_path,
               delete_url: ->(record){ admin_type_path(record) }
              ) %>

    <%= render 'admin/shared/confirm_delete_modal', modal_title: 'გსურთ ტიპის წაშლა?', modal_text: 'ეს ტიპი წაიშლება სრულად' %>
  </div>
<main>
